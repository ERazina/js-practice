function checkAge(age) {    if (age > 18) {        return true;    } else {        return confirm('Родители разрешили?');    }}//та же функция только с тернарным операторомfunction checkAge{    return (age > 18) ? true : confirm ('Родители разрешили?');}//та же функция с ||function checkAge{    return (age > 18) || confirm ('Родители разрешили?');}//функция возвращает меньше числоfunction ReturnMin( a, b){    if ( a > b){        return a;    }    else{        return b;    }}//функция берет 2 аргумента и возводит одно значение в степень другого значенияfunction pow( x, n ){    if ( n < 1 ){        alert("N должно быть больше 0");    }    else{    var result;    result = ( x * x ) * n;    }    return result;}var x = prompt("Введите значение x");var n = prompt("Введите значение n");function pow();